import 'package:flutter_test/flutter_test.dart';

void main() async {
  group(
    'Home Screen',
    () {
      testWidgets(
        'There should only be visible 5 TodoCard\'s on screen is the same time',
        (WidgetTester tester) async {},
      );
    },
  );
}
